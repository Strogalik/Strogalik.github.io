import { PageHeader } from '../components/PageHeader';
import { useAlertRules } from '../api/queries';
export function AdminAlertsPage(){const data=useAlertRules().data??[];return <div className="page"><PageHeader kicker="Уведомления" title="Правила алертов" description="Пороговые значения должны быть понятны руководителю и администратору, а каналы — управляться независимо от текста события."/><section className="panel alert-rules"><div className="panel__header"><div><span className="eyebrow">Активные правила</span><h2>Пороги и каналы</h2></div></div>{data.map(x=><article className="alert-rule" key={x.id}><div className="alert-rule__copy"><span>{x.module}</span><strong>{x.title}</strong><small>Порог: {x.threshold}</small></div><div className="channel-chips">{x.channels.map(c=><span key={c}>{c}</span>)}</div><label className="toggle"><input type="checkbox" defaultChecked={x.enabled}/><span aria-hidden="true"/></label></article>)}</section></div>}
