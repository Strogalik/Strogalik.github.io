import { useState } from 'react';
import { Link } from 'react-router-dom';
import logo from '../assets/tms-logo.svg';
import { Icon } from '../components/Icon';
export function TwoFactorPage(){const [resent,setResent]=useState(false);return <main className="auth-page auth-page--center"><section className="auth-card auth-card--2fa"><div className="auth-brand"><img src={logo} alt="TMS"/><div><strong>TMS ASUB</strong><span>Безопасный вход</span></div></div><div className="auth-copy"><span className="eyebrow">Двухфакторная проверка</span><h1>Введите код</h1><p>{resent?'Новый код отправлен. Используйте последнее сообщение второго фактора.':'Код отправлен в подключённый второй фактор. Не сообщайте его другим людям.'}</p></div><div className="otp-grid" aria-label="Код подтверждения">{['4','8','2','1','9','6'].map((v,i)=><input key={i} inputMode="numeric" maxLength={1} defaultValue={v} aria-label={`Цифра ${i+1}`}/>)}</div><Link to="/dashboard" className="btn btn--primary auth-submit"><Icon name="check"/>Продолжить</Link><button type="button" className="auth-link" onClick={()=>setResent(true)}>{resent?'Код отправлен повторно':'Отправить код повторно'}</button><Link to="/login" className="auth-link">Вернуться ко входу</Link></section></main>}
